# PostgreSQL connection (leave unset to use local SQLite)
# DATABASE_URL=postgresql://user:pass@localhost:5432/space_ai

# MLflow tracking URI (leave unset for local ./mlruns/ directory)
# MLFLOW_TRACKING_URI=http://localhost:5000
